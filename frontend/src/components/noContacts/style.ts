import styled from 'styled-components';

const NoContactsContainer = styled.div`
    color: #f9f9f9;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;

    text-align: center;

    > div {
        width: 35%;

        img {
            width: 100%;
        }
    }
`;

export default NoContactsContainer;
