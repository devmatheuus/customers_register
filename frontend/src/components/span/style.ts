import styled from 'styled-components';

export const Span = styled.span`
    color: #ff0000;
    font-size: 1.1rem;
    font-weight: 400;

    align-self: start;

    gap: 2px;

    transform: translateY(-25px);
`;
