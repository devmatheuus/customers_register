import styled from 'styled-components';

export const Span = styled.span`
    color: #ff0000;
    font-size: 1.1rem;
    font-weight: 400;

    align-self: start;

    padding: 10px 0 0 0;

    transform: translateY(-20px);
`;
